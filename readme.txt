this is a readme file
hello thes
git remote add origin https://github.com/2991535823/study_git.git
main branch

# 第一次推送到远程时需要指定具体的分支，因为远程仓库并没有这个分支  
# 使用 -u 记录 push 到远端分支的默认值，将来直接 git push 即可  
git push -u origin "master" 